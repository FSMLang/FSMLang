/**
	parser-test13.c

	This file automatically generated by FSMLang
*/

#include "parser-test13_priv.h"
#include <stddef.h>

#ifndef DBG_PRINTF
#define DBG_PRINTF(...)
#endif

extern NEW_MACHINE_SUB_FSM_IF newMachine_subMachine1_sub_fsm_if;
extern NEW_MACHINE_SUB_FSM_IF newMachine_subMachine2_sub_fsm_if;

static NEW_MACHINE_EVENT s1_stateFn(pNEW_MACHINE,NEW_MACHINE_EVENT);
static NEW_MACHINE_EVENT s2_stateFn(pNEW_MACHINE,NEW_MACHINE_EVENT);
static NEW_MACHINE_EVENT s3_stateFn(pNEW_MACHINE,NEW_MACHINE_EVENT);
static const NEW_MACHINE_STATE_FN newMachine_state_fn_array[newMachine_numStates] = 
{
	  s1_stateFn
	, s2_stateFn
	, s3_stateFn
};


const pNEW_MACHINE_SUB_FSM_IF newMachine_sub_fsm_if_array[THIS(numSubMachines)] =
{
	&newMachine_subMachine1_sub_fsm_if
	, &newMachine_subMachine2_sub_fsm_if
};

NEW_MACHINE newMachine = {
	newMachine_s1,
	THIS(e1),
	&newMachine_state_fn_array,
	&newMachine_sub_fsm_if_array,
	newMachineFSM
};

pNEW_MACHINE pnewMachine = &newMachine;

void run_newMachine(NEW_MACHINE_EVENT e)
{
	if (pnewMachine)
	{
		pnewMachine->fsm(pnewMachine,e);
	}
}

static NEW_MACHINE_EVENT findAndRunSubMachine(pNEW_MACHINE,NEW_MACHINE_EVENT);

static NEW_MACHINE_EVENT checkWhetherEventIsHandledInAllStates(pNEW_MACHINE,NEW_MACHINE_EVENT);

#ifndef EVENT_IS_NOT_EXCLUDED_FROM_LOG
#define EVENT_IS_NOT_EXCLUDED_FROM_LOG(e) ((e) == (e))
#endif
void newMachineFSM(pNEW_MACHINE pfsm, NEW_MACHINE_EVENT event)
{
	NEW_MACHINE_EVENT e = event;

	while (e != newMachine_noEvent) {

#ifdef NEW_MACHINE_DEBUG
if (EVENT_IS_NOT_EXCLUDED_FROM_LOG(e))
{
	DBG_PRINTF("event: %s; state: %s"
,NEW_MACHINE_EVENT_NAMES[e]
,NEW_MACHINE_STATE_NAMES[pfsm->state]
);
}
#endif

		/* This is read-only data to facilitate error reporting in action functions */
		pfsm->event = e;

		if (e < newMachine_noEvent)
		{
			if ((e = checkWhetherEventIsHandledInAllStates(pfsm,e)) != THIS(noEvent))
				e = ((* (*pfsm->statesArray)[pfsm->state])(pfsm,e));
		}
		else
		{
				e = findAndRunSubMachine(pfsm, e);
		}

	}
}


static NEW_MACHINE_EVENT findAndRunSubMachine(pNEW_MACHINE pfsm, NEW_MACHINE_EVENT e)
{
	for (NEW_MACHINE_SUB_MACHINES machineIterator = THIS(firstSubMachine);
	     machineIterator < THIS(numSubMachines);
	     machineIterator++
	    )
	{
			if (
			   ((*pfsm->subMachineArray)[machineIterator]->first_event <= e)
			   && ((*pfsm->subMachineArray)[machineIterator]->last_event > e)
			    )
			{
				return ((*(*pfsm->subMachineArray)[machineIterator]->subFSM)(e));
			}
	}

	return THIS(noEvent);

}

static NEW_MACHINE_EVENT checkWhetherEventIsHandledInAllStates(pNEW_MACHINE pfsm,NEW_MACHINE_EVENT e)
{
	NEW_MACHINE_EVENT retVal;
	NEW_MACHINE_STATE new_s = pfsm->state;

	switch (e)
	{
		case newMachine_e2:
			retVal = THIS(noEvent);
			DBG_PRINTF("newMachine_noAction");
			new_s = UFMN(transitionFn1)(pfsm,e);
			break;
		case newMachine_e3:
			retVal = THIS(noEvent);
			DBG_PRINTF("newMachine_noAction");
			new_s = UFMN(transitionFn1)(pfsm,e);
			break;
		default:
			retVal = e;
			break;
	}

	if (pfsm->state != new_s)
	{
		newMachine_baz(pfsm, new_s);
		pfsm->state = new_s;
	}


	 return retVal;

}

static NEW_MACHINE_EVENT s1_stateFn(pNEW_MACHINE pfsm,NEW_MACHINE_EVENT e)
{
	NEW_MACHINE_EVENT retVal = THIS(noEvent);
	NEW_MACHINE_STATE new_s = newMachine_s1;

	switch(e)
	{
	case THIS(e1):
		retVal = UFMN(a1)(pfsm);
		new_s = newMachine_s1;
		break;
	default:
		DBG_PRINTF("newMachine_noAction");
		break;
	}


	if (newMachine_s1 != new_s)
	{
		UFMN(baz)(pfsm, new_s);
		pfsm->state = new_s;

	}


	return retVal;
}

static NEW_MACHINE_EVENT s2_stateFn(pNEW_MACHINE pfsm,NEW_MACHINE_EVENT e)
{
	NEW_MACHINE_EVENT retVal = THIS(noEvent);
	NEW_MACHINE_STATE new_s = newMachine_s2;

	switch(e)
	{
	case THIS(e1):
		retVal = UFMN(a2)(pfsm);
		new_s = UFMN(transitionFn)(pfsm,e);
		break;
	default:
		DBG_PRINTF("newMachine_noAction");
		break;
	}


	if (newMachine_s2 != new_s)
	{
		UFMN(baz)(pfsm, new_s);
		pfsm->state = new_s;

	}


	return retVal;
}

static NEW_MACHINE_EVENT s3_stateFn(pNEW_MACHINE pfsm,NEW_MACHINE_EVENT e)
{
	NEW_MACHINE_EVENT retVal = THIS(noEvent);
	NEW_MACHINE_STATE new_s = newMachine_s3;

	switch(e)
	{
	case THIS(e1):
		retVal = UFMN(a2)(pfsm);
		new_s = UFMN(transitionFn)(pfsm,e);
		break;
	default:
		DBG_PRINTF("newMachine_noAction");
		break;
	}


	if (newMachine_s3 != new_s)
	{
		UFMN(baz)(pfsm, new_s);
		pfsm->state = new_s;

	}


	return retVal;
}


NEW_MACHINE_EVENT __attribute__((weak)) UFMN(a1)(FSM_TYPE_PTR pfsm)
{
	DBG_PRINTF("weak: %s", __func__);
	(void) pfsm;
	return THIS(noEvent);
}

NEW_MACHINE_EVENT __attribute__((weak)) UFMN(a2)(FSM_TYPE_PTR pfsm)
{
	DBG_PRINTF("weak: %s", __func__);
	(void) pfsm;
	return THIS(noEvent);
}


#ifdef NEW_MACHINE_DEBUG
char *NEW_MACHINE_EVENT_NAMES[] = {
	 "newMachine_e1"
	,"newMachine_e2"
	,"newMachine_e3"
	,"newMachine_e4"
	, "newMachine_noEvent"
	, "newMachine_numEvents"
	, "newMachine_subMachine1_ee1"
	, "newMachine_subMachine1_ee2"
	, "newMachine_subMachine1_ee3"
	, "newMachine_subMachine1_noEvent"
	, "newMachine_subMachine2_eee1"
	, "newMachine_subMachine2_eee2"
	, "newMachine_subMachine2_eee3"
	, "newMachine_subMachine2_noEvent"
};

char *NEW_MACHINE_STATE_NAMES[] = {
	 "newMachine_s1"
	,"newMachine_s2"
	,"newMachine_s3"
};

#endif
