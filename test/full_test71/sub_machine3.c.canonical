/**
	sub_machine3.c

	This file automatically generated by FSMLang
*/

#include "sub_machine3_priv.h"
#include <stddef.h>


#ifndef DBG_PRINTF
#define DBG_PRINTF(...)
#endif

static TOP_LEVEL_EVENT sub_machine3FSM(FSM_TYPE_PTR,TOP_LEVEL_EVENT);

static TOP_LEVEL_EVENT s1_stateFn(pSUB_MACHINE3,TOP_LEVEL_EVENT);
static TOP_LEVEL_EVENT s2_stateFn(pSUB_MACHINE3,TOP_LEVEL_EVENT);
static TOP_LEVEL_EVENT s3_stateFn(pSUB_MACHINE3,TOP_LEVEL_EVENT);

static const SUB_MACHINE3_STATE_FN sub_machine3_state_fn_array[sub_machine3_numStates] = 
{
	  s1_stateFn
	, s2_stateFn
	, s3_stateFn
};

TOP_LEVEL_EVENT THIS(sub_machine_fn)(TOP_LEVEL_EVENT e)
{
	return sub_machine3FSM(psub_machine3,e);
}

TOP_LEVEL_SUB_FSM_IF top_level_sub_machine3_sub_fsm_if =
{
		.subFSM = THIS(sub_machine_fn)
	, .first_event = THIS(firstEvent)
	, .last_event = THIS(noEvent)
};

SUB_MACHINE3 sub_machine3 = {
	sub_machine3_s1,
	THIS(e1),
	&sub_machine3_state_fn_array,
	sub_machine3FSM
};

pSUB_MACHINE3 psub_machine3 = &sub_machine3;


#ifndef EVENT_IS_NOT_EXCLUDED_FROM_LOG
#define EVENT_IS_NOT_EXCLUDED_FROM_LOG(e) (e == e)
#endif
TOP_LEVEL_EVENT sub_machine3FSM(pSUB_MACHINE3 pfsm, TOP_LEVEL_EVENT event)
{
	TOP_LEVEL_EVENT e = event;

	while ((e != THIS(noEvent))
	       && (e >= THIS(firstEvent))
	      )
	{

#ifdef TOP_LEVEL_SUB_MACHINE3_DEBUG
if ((EVENT_IS_NOT_EXCLUDED_FROM_LOG(e))
    && (e >= THIS(firstEvent))
    && (e < THIS(noEvent))
   )
{
	DBG_PRINTF("event: %s; state: %s"
,SUB_MACHINE3_EVENT_NAMES[e - THIS(firstEvent)]
,SUB_MACHINE3_STATE_NAMES[pfsm->state]
);
}
#endif

		/* This is read-only data to facilitate error reporting in action functions */
		pfsm->event = e;

		if ((e >= THIS(firstEvent))
		    && (e < THIS(noEvent))
			)
		{
		e = ((* (*pfsm->statesArray)[pfsm->state])(pfsm,e));
		}

	}

	return e == THIS(noEvent) ? PARENT(noEvent) : e;
}

static TOP_LEVEL_EVENT s1_stateFn(pSUB_MACHINE3 pfsm,TOP_LEVEL_EVENT e)
{
	TOP_LEVEL_EVENT retVal = THIS(noEvent);

	switch(e)
	{
	case THIS(e1):
		retVal = UFMN(a1)(pfsm);
		pfsm->state = sub_machine3_s2;
		break;
	default:
		DBG_PRINTF("sub_machine3_noAction");
		break;
	}

	return retVal;
}

static TOP_LEVEL_EVENT s2_stateFn(pSUB_MACHINE3 pfsm,TOP_LEVEL_EVENT e)
{
	TOP_LEVEL_EVENT retVal = THIS(noEvent);

	switch(e)
	{
	case THIS(e2):
		retVal = UFMN(a2)(pfsm);
		pfsm->state = sub_machine3_s3;
		break;
	default:
		DBG_PRINTF("sub_machine3_noAction");
		break;
	}

	return retVal;
}

static TOP_LEVEL_EVENT s3_stateFn(pSUB_MACHINE3 pfsm,TOP_LEVEL_EVENT e)
{
	TOP_LEVEL_EVENT retVal = THIS(noEvent);

	switch(e)
	{
	case THIS(e3):
		retVal = UFMN(a3)(pfsm);
		pfsm->state = sub_machine3_s1;
		break;
	default:
		DBG_PRINTF("sub_machine3_noAction");
		break;
	}

	return retVal;
}


TOP_LEVEL_EVENT __attribute__((weak)) UFMN(a1)(FSM_TYPE_PTR pfsm)
{
	DBG_PRINTF("weak: %s", __func__);
	(void) pfsm;
	return THIS(noEvent);
}

TOP_LEVEL_EVENT __attribute__((weak)) UFMN(a2)(FSM_TYPE_PTR pfsm)
{
	DBG_PRINTF("weak: %s", __func__);
	(void) pfsm;
	return THIS(noEvent);
}

TOP_LEVEL_EVENT __attribute__((weak)) UFMN(a3)(FSM_TYPE_PTR pfsm)
{
	DBG_PRINTF("weak: %s", __func__);
	(void) pfsm;
	return THIS(noEvent);
}


#ifdef TOP_LEVEL_SUB_MACHINE3_DEBUG
char *SUB_MACHINE3_EVENT_NAMES[] = {
	 "sub_machine3_e1"
	,"sub_machine3_e2"
	,"sub_machine3_e3"
	, "sub_machine3_noEvent"
	, "sub_machine3_numEvents"
};

char *SUB_MACHINE3_STATE_NAMES[] = {
	 "sub_machine3_s1"
	,"sub_machine3_s2"
	,"sub_machine3_s3"
};

#endif
