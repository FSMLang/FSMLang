/**
 * ns.kt
 *
 * This file generated by FSMLang
 */

enum class NarcolepticSuperheroState {
    asleep,
    hanging_out,
    hungry,
    sweaty,
    saving_the_world,
    noState
}

enum class NarcolepticSuperheroEvent {
    wake_up,
    work_out,
    eat,
    clean_up,
    nap,
    distress_call,
    complete_mission,
    noEvent
}

data class NarcolepticSuperheroDistress_callData(
    val severity: Int = 0
)

data class NarcolepticSuperheroComplete_missionData(
    val result: Char = '\u0000'
)

sealed class NarcolepticSuperheroFsmEvent {
    object Wake_up : NarcolepticSuperheroFsmEvent()
    object Work_out : NarcolepticSuperheroFsmEvent()
    object Eat : NarcolepticSuperheroFsmEvent()
    object Clean_up : NarcolepticSuperheroFsmEvent()
    object Nap : NarcolepticSuperheroFsmEvent()
    data class Distress_call(val payload: NarcolepticSuperheroDistress_callData = NarcolepticSuperheroDistress_callData()) : NarcolepticSuperheroFsmEvent()
    data class Complete_mission(val payload: NarcolepticSuperheroComplete_missionData = NarcolepticSuperheroComplete_missionData()) : NarcolepticSuperheroFsmEvent()
    object NoEvent : NarcolepticSuperheroFsmEvent()
}

open class NarcolepticSuperheroUserHooks {
    open fun change_into_super_secret_costume(fsm: NarcolepticSuperhero): NarcolepticSuperheroEvent {
        println("NarcolepticSuperhero_change_into_super_secret_costume")
        return NarcolepticSuperheroEvent.noEvent
    }
    open fun update_journal(fsm: NarcolepticSuperhero): NarcolepticSuperheroEvent {
        println("NarcolepticSuperhero_update_journal")
        return NarcolepticSuperheroEvent.noEvent
    }
    open fun decide_rest(fsm: NarcolepticSuperhero): NarcolepticSuperheroState {
        println("NarcolepticSuperhero_decide_rest")
        return NarcolepticSuperheroState.noState
    }
    open fun translate_distress_call(fsm: NarcolepticSuperhero, payload: NarcolepticSuperheroDistress_callData): NarcolepticSuperheroDistress_callData {
        println("NarcolepticSuperhero_translate_distress_call")
        return payload
    }
    open fun handle_mission(fsm: NarcolepticSuperhero, payload: NarcolepticSuperheroComplete_missionData): NarcolepticSuperheroComplete_missionData {
        println("NarcolepticSuperhero_handle_mission")
        return payload
    }
}

class NarcolepticSuperhero(val hooks: NarcolepticSuperheroUserHooks = NarcolepticSuperheroUserHooks()) {
    var state: NarcolepticSuperheroState = NarcolepticSuperheroState.asleep
    var event: NarcolepticSuperheroEvent = NarcolepticSuperheroEvent.noEvent

    private fun translateEventData(event: NarcolepticSuperheroFsmEvent) {
        when (event) {
            is NarcolepticSuperheroFsmEvent.Distress_call -> hooks.translate_distress_call(this, event.payload)
            is NarcolepticSuperheroFsmEvent.Complete_mission -> hooks.handle_mission(this, event.payload)
            else -> {}
        }
    }

    private fun NarcolepticSuperheroFsmEvent.toEventEnum(): NarcolepticSuperheroEvent = when (this) {
        is NarcolepticSuperheroFsmEvent.Wake_up -> NarcolepticSuperheroEvent.wake_up
        is NarcolepticSuperheroFsmEvent.Work_out -> NarcolepticSuperheroEvent.work_out
        is NarcolepticSuperheroFsmEvent.Eat -> NarcolepticSuperheroEvent.eat
        is NarcolepticSuperheroFsmEvent.Clean_up -> NarcolepticSuperheroEvent.clean_up
        is NarcolepticSuperheroFsmEvent.Nap -> NarcolepticSuperheroEvent.nap
        is NarcolepticSuperheroFsmEvent.Distress_call -> NarcolepticSuperheroEvent.distress_call
        is NarcolepticSuperheroFsmEvent.Complete_mission -> NarcolepticSuperheroEvent.complete_mission
        else -> NarcolepticSuperheroEvent.noEvent
    }

    fun dispatch(event: NarcolepticSuperheroFsmEvent) {
        translateEventData(event)
        var currentEvent = event.toEventEnum()
        this.event = currentEvent
        while (currentEvent != NarcolepticSuperheroEvent.noEvent) {
            val prevState = state
            when (state) {
                NarcolepticSuperheroState.asleep -> when (currentEvent) {
                    NarcolepticSuperheroEvent.wake_up -> {
                        currentEvent = NarcolepticSuperheroEvent.noEvent
                        state = NarcolepticSuperheroState.hanging_out
                    }
                    NarcolepticSuperheroEvent.distress_call -> {
                        currentEvent = hooks.change_into_super_secret_costume(this)
                        state = NarcolepticSuperheroState.saving_the_world
                    }
                    else -> currentEvent = NarcolepticSuperheroEvent.noEvent
                }
                NarcolepticSuperheroState.hanging_out -> when (currentEvent) {
                    NarcolepticSuperheroEvent.work_out -> {
                        currentEvent = NarcolepticSuperheroEvent.noEvent
                        state = NarcolepticSuperheroState.hungry
                    }
                    NarcolepticSuperheroEvent.distress_call -> {
                        currentEvent = hooks.change_into_super_secret_costume(this)
                        state = NarcolepticSuperheroState.saving_the_world
                    }
                    else -> currentEvent = NarcolepticSuperheroEvent.noEvent
                }
                NarcolepticSuperheroState.hungry -> when (currentEvent) {
                    NarcolepticSuperheroEvent.eat -> {
                        currentEvent = NarcolepticSuperheroEvent.noEvent
                        state = NarcolepticSuperheroState.hanging_out
                    }
                    NarcolepticSuperheroEvent.distress_call -> {
                        currentEvent = hooks.change_into_super_secret_costume(this)
                        state = NarcolepticSuperheroState.saving_the_world
                    }
                    else -> currentEvent = NarcolepticSuperheroEvent.noEvent
                }
                NarcolepticSuperheroState.sweaty -> when (currentEvent) {
                    NarcolepticSuperheroEvent.clean_up -> {
                        currentEvent = NarcolepticSuperheroEvent.noEvent
                        state = hooks.decide_rest(this)
                    }
                    NarcolepticSuperheroEvent.distress_call -> {
                        currentEvent = hooks.change_into_super_secret_costume(this)
                        state = NarcolepticSuperheroState.saving_the_world
                    }
                    else -> currentEvent = NarcolepticSuperheroEvent.noEvent
                }
                NarcolepticSuperheroState.saving_the_world -> when (currentEvent) {
                    NarcolepticSuperheroEvent.distress_call -> {
                        currentEvent = hooks.change_into_super_secret_costume(this)
                        state = NarcolepticSuperheroState.saving_the_world
                    }
                    NarcolepticSuperheroEvent.complete_mission -> {
                        currentEvent = hooks.update_journal(this)
                        state = NarcolepticSuperheroState.sweaty
                    }
                    else -> currentEvent = NarcolepticSuperheroEvent.noEvent
                }
                else -> currentEvent = NarcolepticSuperheroEvent.noEvent
            }
            this.event = currentEvent
        }
    }
}
