/**
	subMachine2.h

	This file automatically generated by FSMLang
*/

#ifndef _SUBMACHINE2_H_
#define _SUBMACHINE2_H_

#include "parser-test13.h"

#ifdef SUB_MACHINE2_DEBUG
#include <stdio.h>
#include <stdlib.h>
#endif

#define DECLARE_SUB_MACHINE2_MACHINE(A) \
SUB_MACHINE2 A =\
{\
	subMachine2_sss1,\n	subMachine2_newMachine_noEvent,\
	&subMachine2_state_fn_array,\
	subMachine2FSM\
};\
SUB_MACHINE2 *p##A = &(A);

/*
	sub-machine events are included in the top-level machine event enumeration.
	These macros set the appropriate names for events from THIS machine
	and those from the PARENT machine.

	They may be turned off as needed.
*/
#ifndef NO_EVENT_CONVENIENCE_MACROS
#undef THIS
#define THIS(A) newMachine_subMachine2_##A
#define PARENT(A) newMachine_##A
#endif

#ifdef SUB_MACHINE2_DEBUG
extern char *SUB_MACHINE2_EVENT_NAMES[];
#endif

typedef enum {
	 subMachine2_sss1
	, subMachine2_sss2
	, subMachine2_sss3
	, subMachine2_numStates
} SUB_MACHINE2_STATE;

#ifdef SUB_MACHINE2_DEBUG
extern char *SUB_MACHINE2_STATE_NAMES[];
#endif

typedef struct _subMachine2_struct_ SUB_MACHINE2, *pSUB_MACHINE2;
#undef FSM_TYPE_PTR
#define FSM_TYPE_PTR pSUB_MACHINE2
extern SUB_MACHINE2 subMachine2;

extern pSUB_MACHINE2 psubMachine2;

typedef NEW_MACHINE_EVENT (*SUB_MACHINE2_ACTION_FN)(pSUB_MACHINE2);

typedef NEW_MACHINE_EVENT (*SUB_MACHINE2_FSM)(pSUB_MACHINE2,NEW_MACHINE_EVENT);

NEW_MACHINE_EVENT subMachine2FSM(pSUB_MACHINE2,NEW_MACHINE_EVENT);

typedef NEW_MACHINE_EVENT (*SUB_MACHINE2_STATE_FN)(pSUB_MACHINE2,NEW_MACHINE_EVENT);

extern const SUB_MACHINE2_STATE_FN subMachine2_state_fn_array[subMachine2_numStates];

struct _subMachine2_struct_ {
	SUB_MACHINE2_STATE					state;
	NEW_MACHINE_EVENT					event;
	SUB_MACHINE2_STATE_FN const	(*statesArray)[subMachine2_numStates];
	SUB_MACHINE2_FSM						fsm;
};

NEW_MACHINE_EVENT subMachine2_sss1_stateFn(pSUB_MACHINE2,NEW_MACHINE_EVENT);
NEW_MACHINE_EVENT subMachine2_sss2_stateFn(pSUB_MACHINE2,NEW_MACHINE_EVENT);
NEW_MACHINE_EVENT subMachine2_sss3_stateFn(pSUB_MACHINE2,NEW_MACHINE_EVENT);

NEW_MACHINE_EVENT subMachine2_aaa1(pSUB_MACHINE2);
NEW_MACHINE_EVENT subMachine2_aaa2(pSUB_MACHINE2);

#endif
